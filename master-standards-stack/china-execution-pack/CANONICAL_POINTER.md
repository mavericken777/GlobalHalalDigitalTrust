# Duplicate pack notice

This directory (`china-execution-pack/`) is a **lineage working copy**.

Primary pack name going forward: `master-standards-stack/CHINA_EXECUTION_PACK/`.

Both trees are specification-only. Neither is a running execution system. File-level merge is an open engineering task, not a silent rewrite.
