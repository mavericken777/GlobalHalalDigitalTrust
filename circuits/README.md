# Circuit specs

`HalalWhitelistVerifier.circom` is a **source specification** for a Poseidon Merkle membership proof.

It is not compiled, has no trusted setup, and is not wired into the gateway. Presence of `.circom` is not a zero-knowledge deployment.
